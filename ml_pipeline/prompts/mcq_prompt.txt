You are an expert assessment designer for India's Official Statistical System.

Given the training material below, generate exactly {num_questions} multiple-choice
questions that test the competency: {competency}
Target difficulty for every question: {difficulty}

Rules:
1. Each question MUST be answerable ONLY from the given content (no outside knowledge).
2. Each question MUST have exactly 4 options and exactly ONE correct answer
   (no ambiguous wording, no hidden second correct option).
3. Distractors (wrong options) must be plausible but demonstrably incorrect
   given the content below.
4. Test understanding, not just memorization.
5. Do not invent facts that are not present in the content.

Content:
{content}

Output ONLY a valid JSON array, with no markdown fences and no text before or
after it. Each element must have exactly these fields:
[
  {{
    "question": "...",
    "options": ["...", "...", "...", "..."],
    "correct_answer": "A",
    "explanation": "...",
    "competency": "{competency}",
    "difficulty": "{difficulty}"
  }}
]

"correct_answer" must be one of "A", "B", "C", "D", corresponding to the
0-indexed position in "options" (A = options[0], B = options[1], etc.).
