"""
GyanSetu ML/AI pipeline.
Owners: Likhita + Ankit (see PROJECT_CONTEXT.md / Build Guide §11).

Currently contains only what ML-001 needs (config + Groq connectivity
test). ML-002+ modules (document_processor, mcq_generator, mcq_validator,
chatbot, etc.) are not implemented yet — do not import them.
"""
